export enum ENUM_SCHEDULE_STATUS {
  PENDING = 'pending',
  COMPLETED = 'completed',
  CANCELED = 'canceled'
}

export enum ENUM_PLAN_STATUS {
  DONE = 'done',
  INPROGRESS = 'inprogress',
  INCOMING = 'incoming'
}

export enum ENUM_MEMBER_PERMISSION {
  EDIT = 'edit',
  VIEW = 'view'
}

export enum ENUM_SCHEDULE_TAB {
  UPCOMING = 'upcoming',
  COMPLETED = 'completed',
  IN_PROGRESS = 'in_progress'
}
